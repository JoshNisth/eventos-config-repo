spring:
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/

global:
  banner: "Configuración centralizada activa"
